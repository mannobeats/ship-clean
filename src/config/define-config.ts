import type { ShipCleanConfig, ShipCleanPreset } from "../rules/types.js";

export const defineConfig = (config: ShipCleanConfig): ShipCleanConfig => config;

export const definePreset = (preset: ShipCleanPreset): ShipCleanPreset => preset;
